# --- Segreti: mai nel repo ---
# Permette di RICEVERE push. Fuori dal repo per non esporre project id e quota.
google-services.json
# Permette di INVIARE push: secret vero. Montata come volume Docker sull'Hub.
*-service-account*.json
# Come la scarica la console Firebase, che il pattern sopra non intercetta.
*firebase-adminsdk*.json
*.iam.gserviceaccount.com.json
hub/secrets/
.env
!tailscale.env.template

# --- Android / Gradle ---
*.iml
.gradle/
local.properties
android-app/build/
android-app/app/build/
.kotlin/
android-app/captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.keystore
!debug.keystore

# --- Node ---
node_modules/
npm-debug.log*
hub/data/

# --- IDE / OS ---
.idea/
.vscode/
*.swp
Thumbs.db
desktop.ini

# --- Appunti di lavoro ---
# Diario di bordo delle sessioni: italiano, dettagli di rete e di macchina.
# Resta locale. Le convenzioni che servono a chi lavora sul progetto stanno
# in AGENTS.md.
/HANDOFF.md
